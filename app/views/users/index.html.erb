<h1>All Users</h1>
<% @users.each do | user |%>
  <p><%= user.username %></p>
  <%= gravatar_for user, size: 150 %>
  <p>Joined in <%= time_ago_in_words(user.created_at) %></p>
<% end %>

<div class="apple_pagination">
  <%= will_paginate @articles %>
</div>